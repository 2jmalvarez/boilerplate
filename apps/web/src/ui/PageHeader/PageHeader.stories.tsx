import type { Meta, StoryObj } from "@storybook/react-vite"; import { Button } from "../Button/Button"; import { PageHeader } from "./PageHeader";
const meta = { title: "Design System/UI/PageHeader", component: PageHeader, args: { eyebrow: "Operaciones / Tareas", title: "Panel de tareas", description: "Centraliza el seguimiento de los pendientes del equipo." } } satisfies Meta<typeof PageHeader>; export default meta; type Story = StoryObj<typeof meta>; export const Default: Story = {}; export const WithAction: Story = { args: { action: <Button variant="primary">Nueva tarea</Button> } };
