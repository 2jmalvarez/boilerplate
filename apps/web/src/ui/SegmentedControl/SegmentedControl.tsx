import { segmentedControlVariants } from "./SegmentedControl.variant"; import type { SegmentedControlProps } from "./SegmentedControl.types"; import "./SegmentedControl.css";
export function SegmentedControl<T extends string>({ "aria-label": ariaLabel, compact, onChange, options, value }: Readonly<SegmentedControlProps<T>>) { return <div aria-label={ariaLabel} className={segmentedControlVariants({ compact })} role="group">{options.map((option) => <button aria-pressed={value === option.value} className={value === option.value ? "segmented-control-active" : ""} disabled={option.disabled} key={option.value} type="button" onClick={() => onChange(option.value)}>{option.label}</button>)}</div>; }
